name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: "27371bb4-cfcd-436e-9b9e-a088b17e15e6" #TODO: make this secret
          heroku_app_name: "shilengae-server" #Must be unique in Heroku
          heroku_email: "aman.teferi.80@gmail.com"
